# virtualenv
just test


### Create virtualenv
`$ virtualenv learning`

### Activate to virtualenv
```
$ cd learning
$ source bin/active
```

### Install packages requirement
`$ pip install -r packages.txt`

### Run code
`$ python src/mode/mode.py`
